{
  "name": "db-server",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf ./dist && tsc",
    "watch": "tsc -w",
    "dev": "tsnd --respawn src/index.ts"
  },
  "devDependencies": {
    "nodemon": "^2.0.12",
    "ts-node": "^10.1.0",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.3.5"
  }
}
